{
  "name": "restaurant-management-system",
  "version": "1.0.0",
  "description": "Restaurant Management System - Sprint 1 Implementation",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.public.json",
    "build:server": "tsc",
    "build:client": "tsc -p tsconfig.public.json",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "watch": "tsc --watch & tsc -p tsconfig.public.json --watch"
  },
  "keywords": ["restaurant", "management", "system"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}

